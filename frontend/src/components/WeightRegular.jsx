import React from 'react';

const WeightRegular = ({ weight }) => {
  return (
    <div style={{ fontWeight: weight }}>
      {/* Content for WeightRegular component */}
      {/* You can customize the content based on your design */}
      Regular Weight Text
    </div>
  );
};

export default WeightRegular;
